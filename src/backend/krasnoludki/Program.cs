using krasnoludki.db;
using krasnoludki.Repositories;

namespace Start {
    class Start {
        public static async Task<int> Main(string[] args) {
            /*MainRepository m = new MainRepository();
            
            var (dwarfs, houses, deposits) = await m.GetDataSetupFromDB();

            Console.WriteLine($"Pobrano {dwarfs.Count} krasnoludków.");
            Console.WriteLine($"Pobrano {houses.Count} domów.");
            Console.WriteLine($"Pobrano {deposits.Count} złóż.");


            foreach (var d in dwarfs)
            {
                Console.WriteLine($"Krasnolud: {d.Name}");
            }
            return 0;*/
        }
    }
}